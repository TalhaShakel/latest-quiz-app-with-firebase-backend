class FeatureConfig {

  /* 
    Multi-language feature (If this is turned false, language icon won't show anywhere in the app)
    App will continue with default 'English' Language.
  */
  static const bool multiLanguageEnabled = true;


  /* 
    Enable/Disable Fb login if required.
    If you set the value 'false' then you don't have follow the fb login steps from the doc.
  */
  static const bool fbLoginEnabled = true;

  //Bookmark Question feature for users
  static const bool bookmarkQuestionEnabled = true;

  //Completed quizes
  static const bool completedQuizzesEnabled = true;

  //Points History for users
  static const bool pointsHistoryEnabled = true;

  //User Strength
  static const bool userStrengthEnabled = true;

}